import 'package:flutter/material.dart';

mixin ProjectMargins {
  static const cardMargin = EdgeInsets.all(15);
  static final roundedRectangleBorder =
      RoundedRectangleBorder(borderRadius: BorderRadius.circular(10));
}
